# longestCommonPrefix
A function that finds the longest common prefix of several strings in a vector.

# Example:
Input                     Output
[flow, flew, flight]       "fl"
[car, cross]                "c"
[vision, visor, visa]      "vis"
