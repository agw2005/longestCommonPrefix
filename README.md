# longestCommonPrefix
A function that finds the longest common prefix of several strings in a vector.

# Example:
Input:
[flow, flew, flight]
[car, cross]
[vision, visor, visa]

Output:
"fl"
"c"
"vis"
